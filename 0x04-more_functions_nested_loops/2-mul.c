#include "main.h"
/**
 * mul - print a ,multiple of two integers
 * @a: one of the integer
 * @b: the other integer to be multplied
 * Return: multiple of two integer
 */
int mul(int a, int b)
{
	return (a * b);
}
