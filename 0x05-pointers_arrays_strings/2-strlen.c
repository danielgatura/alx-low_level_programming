#include "main.h"
#include <stdio.h>
/*
 * _strlen- `gives the length of string
 * @str: the string to get the length
 * return: the lenth
 */
int _strlen(char *s)
{
	int length;

	while (*str++)
		length++;
	return (length);
}
